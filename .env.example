# Copy this file to .env and adjust values for your setup.
# If PM_DATA_DIR is not set, the system falls back to demo/ inside the project.
PM_DATA_DIR=/path/to/your/project-manager-data

# Optional defaults (reduce input when adding requirements)
PM_DEFAULT_PROJECT=官方网站
PM_DEFAULT_OWNER=小王